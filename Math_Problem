// Problem - > https://photos.app.goo.gl/G1do65TQ631oBDUt7

#include <stdio.h>

int main() 
{
    float a=1,b=1,c=1,d=1,e=1;

    for(a=1;a<=9;a++)
    {
        for(b=01;b<=9;b++)
        {
            if(a!=b)
            for(c=01;c<=9;c++)
            {
                if(a!=c && b!=c)
                for(d=01;d<=9;d++)
                {
                    if(a!=d && b!=d && c!=d)
                    for(e=01;e<=9;e++)
                    { 
                        if(a!=e && b!=e && c!=e && d!=e)
                        if( (a+(b*c)-d   == 10) && ((e/d)+(a*b) == 12) && (c+(e*(b/d)) ==  8))
                        {
                            //printf( "%f ")
                            printf("a=%.0f b=%.0f c=%.0f d=%.0f e=%.0f f=%.0f \n",a,b,c,d,e,a-d+e+c+b);
                            //printf("%f   %f    %f   %f\n",a+(b*c)-d,(e/d)+(a*b),c+(e*(b/d)),a-d+e+c+b);
                            
                        }

                    }
                    
                }
                
            }
                
        }

    }    

    return 0;
}
